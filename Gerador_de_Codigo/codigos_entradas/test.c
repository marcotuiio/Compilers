int main() {
    int a = 10;
    // // scanf("%d", &a);
    int b = 40;
    // int c;
    // c = a + b * 2;
    // printf("valor %d\n", c);
    // return 0;
    if (a < b) {
        printf("b maior que a\n");
    } else {
        printf("a menor que b\n");
    }
    return 0;
}